<?php

declare(strict_types=1);

namespace App\Controllers\Site;

use App\Services\Site\{{class_name}}PageService;
use Core\Http\Request;
use Core\Http\Response;
use Core\SiteController;
use Core\View\View;

/**
 * {{class_name}} site sayfasini yonetir.
 */
final class {{class_name}} extends SiteController
{
    /**
     * @param View $view Site view goruntuleyicisi.
     * @param {{class_name}}PageService $page Sayfa servis katmani.
     */
    public function __construct(
        View $view,
        private readonly {{class_name}}PageService $page
    ) {
        parent::__construct($view);
    }

    /**
     * Sayfayi goruntuler.
     *
     * @param Request $request Mevcut HTTP istegi.
     * @return Response
     */
    public function index(Request $request): Response
    {
        return $this->renderPageResult(
            $this->page->page($request->path()),
            'pages/{{view_name}}',
            null,
            null,
            '/'
        );
    }
}
